# ==================================================
# Acelo Production Environment Configuration
# ==================================================
# 
# This file contains production-specific environment variables.
# Copy this file to .env.production for your production deployment.
#
# CRITICAL SECURITY NOTICE:
# - All sensitive values must be set as environment variables or secrets
# - Never commit actual production values to version control
# - Use Vercel environment variables or similar secure methods

# ----------------------------------------------
# Environment Identification
# ----------------------------------------------
NODE_ENV=production
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0-beta.1

# ----------------------------------------------
# Supabase Configuration (Production)
# ----------------------------------------------
VITE_SUPABASE_URL=https://your-production-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-production-anon-key

# ----------------------------------------------
# Application URLs (Production)
# ----------------------------------------------
VITE_APP_URL=https://acelo.ai

# ----------------------------------------------
# AI Integration (Production)
# ----------------------------------------------
# CRITICAL: Set OpenAI key as Supabase secret only
# supabase secrets set OPENAI_API_KEY=sk-prod-key-here --project-ref your-production-project
VITE_DEFAULT_AI_MODEL=gpt-4
VITE_AI_MAX_TOKENS=2000
VITE_AI_TEMPERATURE=0.7

# ----------------------------------------------
# Authentication & Security (Production)
# ----------------------------------------------
VITE_SESSION_TIMEOUT=3600
VITE_ENABLE_2FA=true

# ----------------------------------------------
# Monitoring & Analytics (Production)
# ----------------------------------------------
# Full monitoring and analytics in production
VITE_SENTRY_DSN=your-production-sentry-dsn
VITE_SENTRY_ENVIRONMENT=production

# Analytics platforms
VITE_GOOGLE_ANALYTICS_ID=your-production-ga-id
VITE_POSTHOG_KEY=your-production-posthog-key
VITE_MIXPANEL_TOKEN=your-production-mixpanel-token

# Performance monitoring (lower sampling for cost optimization)
VITE_ENABLE_PERFORMANCE_MONITORING=true
VITE_PERFORMANCE_SAMPLE_RATE=0.1

# ----------------------------------------------
# Email & Communication (Production)
# ----------------------------------------------
FROM_EMAIL=noreply@acelo.ai

# ----------------------------------------------
# Development & Testing (Production)
# ----------------------------------------------
# Disable all dev tools in production
VITE_DEBUG_MODE=false
VITE_ENABLE_QUERY_DEVTOOLS=false
VITE_ENABLE_REDUX_DEVTOOLS=false

# Test configuration (disabled)
VITE_ENABLE_MSW=false
VITE_TEST_API_DELAY=0

# Feature flags (conservative in production)
VITE_ENABLE_BETA_FEATURES=false
VITE_ENABLE_EXPERIMENTAL_UI=false

# ----------------------------------------------
# Performance & Optimization (Production)
# ----------------------------------------------
ANALYZE_BUNDLE=false
VITE_ENABLE_SW=true
VITE_SW_CACHE_STRATEGY=stale-while-revalidate
VITE_PRELOAD_ROUTES=true
VITE_PREFETCH_ASSETS=true

# ----------------------------------------------
# CDN & Assets (Production)
# ----------------------------------------------
CDN_URL=https://cdn.acelo.ai
VITE_ASSETS_BASE_URL=https://cdn.acelo.ai/assets

# ----------------------------------------------
# API Rate Limiting (Production)
# ----------------------------------------------
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_ENABLE=true

# ----------------------------------------------
# Compliance & Legal (Production)
# ----------------------------------------------
VITE_ENABLE_COOKIE_CONSENT=true
VITE_PRIVACY_POLICY_URL=https://acelo.ai/privacy
VITE_TERMS_OF_SERVICE_URL=https://acelo.ai/terms

# Data retention (full production retention)
USER_DATA_RETENTION_DAYS=2555  # 7 years for compliance
LOG_RETENTION_DAYS=90
ANALYTICS_RETENTION_DAYS=730   # 2 years

# ----------------------------------------------
# Production-Specific Security
# ----------------------------------------------
# CSP and security headers are managed in vercel.json
# Additional security configurations should be set via:
# - Vercel environment variables
# - Supabase secrets
# - Edge Function environment variables

# ----------------------------------------------
# Backup & Disaster Recovery
# ----------------------------------------------
# Backup configurations (if using custom backup solutions)
# BACKUP_ENABLED=true
# BACKUP_FREQUENCY=daily
# BACKUP_RETENTION_DAYS=30

# ----------------------------------------------
# Scaling & Performance
# ----------------------------------------------
# Database connection settings (for Edge Functions)
# DB_POOL_MIN=5
# DB_POOL_MAX=20
# DB_TIMEOUT=30000

# Cache settings
# CACHE_TTL=3600
# STATIC_CACHE_TTL=31536000  # 1 year

# ----------------------------------------------
# Production Deployment Notes
# ----------------------------------------------
#
# Required Vercel Environment Variables:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - VITE_APP_ENV=production
# - VITE_SENTRY_DSN (optional)
# - VITE_GOOGLE_ANALYTICS_ID (optional)
#
# Required Supabase Secrets:
# - OPENAI_API_KEY
# - SMTP_PASSWORD (if using email)
# - JWT_SECRET (if using custom auth)
#
# Vercel Project Settings:
# - Enable automatic deployments from main branch
# - Set custom domains (acelo.ai, www.acelo.ai)
# - Configure edge functions if needed
# - Set up monitoring and alerts
#
# Post-Deployment Checklist:
# 1. Verify all environment variables are set
# 2. Test authentication flow
# 3. Verify AI functionality works
# 4. Check monitoring is receiving data
# 5. Test performance with staging data
# 6. Verify HTTPS and security headers
# 7. Run security scans
# 8. Set up uptime monitoring